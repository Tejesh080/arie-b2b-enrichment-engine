"""Policy Lab — post-M1 P3.

Visualizes and interprets the already-frozen `bench.multi_seed` benchmark
results (see `docs/benchmark.md`). This package does not run the benchmark,
tune any policy, or touch scoring/calibration/provider code — it reads
`bench/out/multi_seed.json` and renders it.
"""
